- [ ] TODO: fix undo tree. When i undo "u" normal mode binding, i expect my cursor not to move. My cursor jumps around too much. If i select an entry from undo-tree.open then the cursor should go to the point.
- [ ] TODO: fix dw command. This currently delete the word and any punctuation. If i have an input of "[copilot](./copilot.png)and i put the cursor of the "c" of copilot and do a dw, this should delete up to the "]" character.
- [ ] TODO: create our own nerd font library as the current one is broken. This should have the same intefrace as the current library but the Material Design code points are not working as the library has moved them. Please use the Sybols Nerd font in the assets folder to find the code points. You can pretty much copy the library and fix the material design code points.
- [ ] TODO: make the acp buffer scroll as new input comes in
- [ ] TODO: make the acp buffer pretty for tool calls, use ![copilot](copilot.png). Use symbols and clean up the text to make it "clean". Use symbols. use `output.txt`. We should be visually showing linked tool calls etc. Use the nerd font icons. Anything that is in progress have an animated symbol in a way to show that it is doing something
- [ ] TODO: add icons to the git status buffer
- [ ] TODO: flexible auto complete. This should spawn a window of the top x results (configurable in user/autocomplete.rs) below the cursor point. We should allow multiple things to register in the autocomplete. Lets start with a buffer autocomplete which shows results of tokens in the buffer. This should be fast and not block the main thread while getting and displaying the results. We should have a configurable key number that triggers the autocomplete window. When the window is active, we should still be able to type normally, but Ctrl-n, Ctrl-p should cycle the results in the window. Then Ctrl-y to accept the result which puts completes the text in the buffer. We should have an insert_mode command of Ctrl-SPACE to manually trigger the autocomplete window to show. We should have the ability to have the token that it is trying to complete, an icon for the completion provider. Looking forwards when we integrate lsp, we should be able to register this as a provider, and it should be able to submit its own results to the window. 
- [ ] TODO: add a flexible hover window bound to user/vim.rs "K" in normal mode. This should appear above the cursor if there is space to put it abover the cursor, otherwise it should be put below. We should allow multiple providers to be registered as a hover provider. We will start by putting lsp doc hover as a provider, and lsp diagnostics as a hover provider. We should have a "K" binding for hover.focus and Ctrl-n Ctrl-p to to cycle beween providers. Lets add a simple provider for testing that just echos the token you have selected under the cursor. The keybind should be configurable in user/hover.rs. I want this to look like ![hover](./hover.png). I want the order of the providers to be configured in user/hover.rs where an example of this would be [test-hover, lsp, diagnostics]. We should have a fixed numebr of lines that we show, lets say 10 for now, and when you focus in the hover, you should be able to scroll through any extra lines. When you are focused, pressing "ESC" should bring you back to the cursor. If you toggle a hover, moving in the buffer should make the hover disappear.
- [ ] TODO: add lsp integration, please investigate the best lsp client library we should use. we should allow a buffer to start multiple lsp servers if it has been setup to do so. Im thinking markdown has an lsp and we could use harper for spelling and grammar. We should have diagnostics avalible to be shown in the buffer (configurable in user/lsp.rs). Ensure lsp automcomplete suggestions are setup as a provider to the autocomplete system. We should show the suggestion documentaiton icon in the autocomplete window. also add lsp diagnostics to the hover system as well as the
- [ ] TODO: add slim browser buffer
- [ ] TODO: mouse support. We should be able to click on windows for focus
