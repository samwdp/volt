# .sln discovery filters

Type: grilling
Blocked by:

## Parent

[.scratch/csharp-ls-solution/map.md](../map.md)

## Question

When recursively listing `.sln` candidates under a project Workspace root, which directories or patterns should Volt skip (e.g. `.git`, `bin`, `obj`, `node_modules`) so the picker and auto-single path stay useful on real repos?
